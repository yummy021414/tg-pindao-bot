# TG-Android 自动发送 MVP 使用说明

## 当前已实现

- 复用现有 TG Bot，人工维护 App 用户和圈子内容的映射。
- **Android 控制台**（`/android` 或主菜单里的「🤖 Android 控制台」按钮）：发送笔记、同步资料、看映射、看队列、改设置全部点按钮完成，不用记命令。
- `/send` 命令按创建顺序拆成任务；队列串行执行，同一时刻只允许一个任务操作手机。
- 节流保护：两次发送之间随机间隔，并设每日发送上限。
- Bot 与本地手机通过 HTTPS/HTTP 队列接口通信；接口须使用 `ANDROID_WORKER_TOKEN`。
- Worker 使用 ADB + UIAutomator：进入目标用户会话（**进会话前会先在资料页点一次"添加好友"，已是好友则自动跳过**）→ 底部"+" → 笔记 → 勾选已映射内容 → 校验"已选 1 条" → 点底部的笔记/展示/位置/三连完成发送 → 校验成功信号。
- 上传资料后自动把素材同步成 App 里的一条笔记，与发送共用串行队列但不占私信配额；也可以在控制台里手动把资料库中**某个人（关键词）的全部资料**同步成笔记。
- 任务结果写回 `data/bot.json`，并由 Bot 回执到创建任务的 TG 会话。

## Android 控制台（按钮操作）

发 `/android`（或 `/安卓`，或主菜单「🤖 Android 控制台」按钮）打开面板：

- **📤 发送笔记**：选人 → 选内容 → 选形式（笔记/展示/位置/三连），点完即排队。
- **📥 同步资料到笔记**：列出资料库全部关键词（分页），选一个人 → 显示共几组几个文件 → 确认后手机按组建笔记。
- **📤 发送笔记**：从最近圈子线索里选人，再选资料库关键词。日常更推荐直接引用线索消息回复「关键词 1」。
- **📥 同步资料到笔记**：把资料库里**已经有的人**同步到手机 App 笔记，不必再上传一遍。
- **📌 映射列表 / 📋 任务队列**：随时查看；任务队列带刷新按钮和配额状态。
- **⚙️ 设置**：切换「上传资料后自动同步笔记」开关（存数据库，重启不丢）。

圈子线索卡也带按钮：点「发笔记/发展示/发位置/发三连」→ 选资料库关键词 → 排队。引用回复同样可用。不用先 `/binduser`。

## 真机验证状态（OPPO PLW110，1080x2344，包名 `com.bangban.cc`）

控件树可以正常抓取。大部分控件没有 `resource-id`，但 `content-desc` 足够用；会话底部工具栏完全没有描述，只能用坐标。

整条发送链路已在真机跑通，并用测试账号「上海 嘉士伯」完成真实发送：

| 步骤 | 定位方式 | 状态 |
|---|---|---|
| 底部 Tab（消息/联系人/圈子） | `content-desc`，需限定屏幕底部区域 | ✅ |
| 消息列表里的已有会话 | `content-desc` 含用户名，支持滚动 | ✅ |
| 圈子里的人 → 头像 → 个人信息 → 发送消息 | 卡片锚点 + 范围内取头像控件 | ✅ |
| 会话顶部标题 → 个人信息页 | 绝对坐标 `[540, 205]` | ✅ |
| 添加好友 → 申请页"确定" | `content-desc`，两步都是可选步骤 | ✅ |
| 会话底部"+" | 绝对坐标 `[962, 2200]` | ✅ |
| "+"面板里的"笔记" | `content-desc` 精确匹配，限定屏幕下方 | ✅ |
| 内容条目 + 左侧勾选圆圈 | 描述锚点 + 左侧固定 x=75 | ✅ |
| 勾选校验"已选 1 条" | `content-desc` | ✅ |
| 点底部笔记/展示/位置/三连发送 | `content-desc` 精确匹配，限定屏幕最底部 | ✅ |
| 发送成功信号 | 标题栏出现对方昵称 + 会话内出现该内容卡片 | ✅ |

### 发送入口有个坑

会话底部工具栏第三个图标（`[400, 2200]`）打开的面板和"+→笔记"**长得一模一样**，但它是笔记管理入口，没有绑定当前会话：勾选后无论点什么都不会发出去，取消后会退到"我的笔记"页。必须走 **"+" → 笔记**。

另外，面板底部的"笔记/展示/位置/三连"不是切换标签，而是**发送动作本身**——勾选内容后点哪个就以哪种形式发出去并自动返回会话。右上角"已选 N 条"只是查看和增删已选列表，点里面的"确定"不会发送。

`success` 配成了两条选择器，必须同时命中才算成功：标题栏出现对方昵称（证明已退回会话），且会话里存在该内容的卡片。只查内容会误判——分享面板的列表里也有同样的描述。

### 每次发送前自动加好友

两条进会话的路线都会路过对方的个人信息页：已有会话时点顶部标题进入，圈子现找时点头像本来就进这里。Worker 在这一页会尝试点「添加好友」→ 申请页直接点「确定」（验证消息留空）→ 回到资料页点「发送消息」进会话。

这两步配置成了 `optional: true`：**已是好友时资料页没有「添加好友」按钮，会自动跳过**，只多花一次抓屏（约 2 秒）。已发过申请但对方未通过时按钮仍在，会重复发一次申请，App 侧是幂等的。不想要这道工序的话，按 `android-worker.config.example.json` 里 `_readme` 的说明删掉对应步骤即可。

### 已知约束

- **圈子内容流动很快**：几分钟就会被新动态刷走，按昵称在圈子里找人会失败。因此 `recipientRoutes` 先在消息列表找已有会话（聊过一次就会留下记录），找不到再去圈子现找。
- **`adb shell input text` 不支持中文**，真机上只有搜狗输入法。所以不走 App 的搜索框，改用滚动 + 描述匹配。若以后要输入中文，需要装 ADBKeyBoard。
- App 菜单里没有按昵称搜索陌生人的入口，只有"添加好友"（按账号）。

## Bot 端配置与命令

VPS 的 `.env` 新增一条随机长令牌（不要提交到 Git）：

```ini
ANDROID_WORKER_TOKEN=请替换为随机长字符串
ANDROID_DAILY_SEND_LIMIT=30
ANDROID_SEND_MIN_INTERVAL_MS=20000
ANDROID_SEND_MAX_INTERVAL_MS=45000
```

部署并重启 Bot 后，管理员可使用：

```text
/binduser A | App里的用户名称 | 备注
/bindcontent C001 | 上海001 | 圈子中可定位的标题 | 备用位置（可选） | 备注（可选）
/send A 上海001 北京003
/mappings
/sendtasks
```

也支持中文别名：`/绑定用户`、`/绑定内容`、`/发送`、`/映射列表`、`/发送任务`。由于 Telegram 的原生命令只允许 ASCII，建议日常使用英文 `/send` 等命令。

`/send` 和 `/sendtasks` 都会回报今日配额、排队数量和当前冷却剩余时间。

## 本机 Worker 配置

1. 将 `android-worker.config.example.json` 复制为 `android-worker.config.json`，该文件已被 `.gitignore` 排除。
2. 填入 VPS 公开地址、同一份 `workerToken`、手机序列号。
3. Windows 上可直接指定 ADB：

```json
"adbPath": "C:\\Users\\x\\AppData\\Local\\Android\\Sdk\\platform-tools\\adb.exe"
```

4. 先做页面取证，再核对选择器：

```powershell
npm run android:ui 页面名           # dump 控件树 + 截图，输出到 data/ui-capture
npm run android:ui 页面名 -- --tap 400,2200   # 先点一下再抓
```

5. 用演练模式验证整条路径（不会真的发送）：

```powershell
npm run android:probe -- --user "会话名" --content "内容片段" --action 1
```

演练会走到"勾选 + 已选校验"为止，然后点取消退出。看到 `✅ 选择器全部命中` 才说明配置正确。

想单独验证发送本身，加 `--live`，它会真的发出去，**务必只对自己的测试账号用**：

```powershell
npm run android:probe -- --user "会话名" --content "内容片段" --action 1 --live
```

另外 `--stay` 会让演练停在已勾选状态不退出，方便人工接着点下一步做取证。

6. 确认无误后把配置里的 `dryRun` 改成 `false`，再启动：

```powershell
npm run android:worker
```

Worker 会持续轮询；队列要求冷却或已达每日上限时，它不会去碰手机。

## 圈子线索 → 点按钮发送

把 `circleSync.enabled` 设为 `true` 后，Worker 会在空闲时扫描圈子当前可见的发帖用户和笔记摘要，投递到超级管理员的 Bot 对话，默认 3 分钟一次（`intervalMs`）。它只生成"圈子线索卡"，不会创建发送任务，也不会私信任何人。

线索卡自带四个按钮：点「发笔记/发展示/发位置/发三连」→ 从**资料库关键词**里选一个 → 任务排队。不用先绑定通讯录。

老方式仍然可用：引用线索卡回复 `轻语 1`（`1=笔记`、`2=展示`、`3=位置`、`4=三连`）。目标用户来自被引用的线索卡，关键词必须是资料库里已经有的人。

由于圈子刷新很快，建议收到线索卡后尽快回复；超时太久且此前没聊过的人，两条定位路径都会找不到，任务会明确判失败并说明原因。

## 上传资料 → 自动同步成 App 笔记

Bot 保存资料成功后会自动排一条"同步笔记"任务（好评图除外），Worker 在手机上执行：把素材推进相册 → 我的 → 下滑 → 我的笔记 → 右上角 + → 新增笔记 → 文字 → 照片 → 视频 → 保存。同一次上传里按 `batchId` 分组，一组资料对应 App 里的一条笔记。

注意："我的"个人页是 Flutter/WebView 渲染，uiautomator 抓不到控件（dump 出来的是上一个原生窗口），所以进入"我的笔记"的三步全部走固定坐标（含一次盲滑动，步骤配置里的 `swipe: [x1,y1,x2,y2]`）。真机分辨率不同时按 `screenBase` 等比缩放，换设备后需重新核对这三个坐标。进入"我的笔记"页后就是原生控件，后续的 +、新增笔记等都按描述匹配，不受影响。

配了 `ANDROID_WORKER_TOKEN` 就默认开启，设 `ANDROID_NOTE_SYNC=false` 可整体关掉；日常开关用控制台「⚙️ 设置」里的「上传后自动同步」即可，无需改环境变量。

除了上传时自动同步，也可以在控制台「📥 同步资料到笔记」里选一个已有关键词，把这个人库里的全部图片视频（按组）补同步成笔记——适合功能上线前就已上传的历史资料。

同步任务和发送任务共用同一条串行队列，因为它们抢的是同一台手机。但它**只操作自己的账号**，所以不占每日私信配额、也不受发送冷却限制——发送冷却期正好拿来建笔记。

### 素材是怎么进手机的

Bot 只存 Telegram 的 `file_id`，本地没有文件，而 App 的"图片/视频"按钮打开的是系统相册选择器。所以链路是：Worker 通过 `/api/android/media/:fileId`（带 Token 鉴权）下载 → `adb push` 进 `/sdcard/Pictures` 或 `/sdcard/Movies` → 触发媒体扫描入库 → 在选择器里按文件名精确选中 → 任务结束后无论成败都从相册删除并重新扫描。

文件名形如 `tgnote-<taskId>-01.jpg`，而系统选择器每个格子的 `content-desc` 恰好带文件名（`图片1, 2026-08-24 14:22:25, tgnote-xxx-01.jpg`），所以是精确匹配，不依赖排序。

**系统选择器一次最多选 9 个**，素材更多时会自动分批点"图片"多次追加。

保存成功的判定是"我的笔记"页顶部的统计文字：建笔记前读一次条数，保存后必须 +1，否则判失败。

### 中文文案需要 ADBKeyBoard

`adb shell input text` 只能输入 ASCII，中文会被直接丢掉；Android 16 也不允许 adb 写剪贴板（`cmd clipboard` 命令不存在），所以"复制粘贴"这条路走不通。

装了 [ADBKeyBoard](https://github.com/senzhk/ADBKeyBoard) 后，Worker 会在输入前临时切到它、用广播注入文案、然后立刻切回原输入法，日常使用不受影响。没装也不会整单失败：笔记照建、素材照传，只是任务回执里会明确写"文案未同步"。

已在真机验证通过（2026-08-24）：装好 ADBKeyBoard 后跑 `android:notesync --live`，中文文案完整进入笔记正文，笔记条数 76 → 77。

### 单独演练

```powershell
npm run android:notesync -- --files "C:\a.jpg,C:\b.mp4" --caption "测试文案"
```

默认停在保存前不落库；加 `--live` 才会真的保存。素材用完会自动从相册清掉。

## 安全提醒

请勿用主账号做首轮测试；先创建一个测试内容映射和可接收消息的测试用户，确认回执与实际效果一致后再扩大使用。自动化操作第三方社交 App 有封号风险，节流参数不要调得过于激进。
